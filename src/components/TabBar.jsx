import React from "react";

const TabBar = ({ tabs, currentTab }) => {
  return (
    <div id="tab-bar">
      {tabs.map((tab) => (
        <img></img>
      ))}
    </div>
  );
};

export default TabBar;
